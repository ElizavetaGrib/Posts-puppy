import {StyleSheet} from 'react-native';

const color = '#283C0A';

export default StyleSheet.create({
    indicatorColor: {
        color,
    },
    beforeMessage: {
        color,
        fontSize: 20,
        fontStyle: 'italic',
        textAlign: 'center',
    },
});
