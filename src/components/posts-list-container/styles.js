import {StyleSheet} from 'react-native';

const darkGreen = '#283C0A';

export default StyleSheet.create({
    indicatorColor: {
        color: darkGreen,
    },
    beforeMessage: {
        color: darkGreen,
        fontSize: 20,
        fontStyle: 'italic',
        textAlign: 'center',
    },
});
